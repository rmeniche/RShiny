# header

header <- dashboardHeader(title = "Simulateur garde partagée")


sidebar <- dashboardSidebar(
  
)

#

body <- dashboardBody()


# 

ui <- dashboardPage(
  header,
  sidebar,
  body
)